import { useEffect } from 'react';

interface Props {
  value?: any;
}

export default function useBookmarks({ value }: Props) {
  // const [value, ] = useState(false);
  // useEffect(() => {
  //   console.log(value);
  // },[value]);

  return;
};