http_endpoints_common
=====================
This directory contains Rust library packages that contain common and reusable HTTP endpoints. 
These might be things that are incorporated into different services, but otherwise broadly reusable.
User account creation, login, billing, etc.
