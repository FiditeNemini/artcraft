[package]
name = "hashing"
edition = "2018"
version = "0.0.1"
authors = [
    "Brandon Thomas <bt@brand.io>",
    "Brandon Thomas <echelon@gmail.com>",
]
publish = false

[lib]
# NB: Public "hasing" crate is not even used at this point (!!!), so we're fine to use this internally.
name = "hashing"
path = "src/lib.rs"

[dependencies]
# Internal
errors = { path = "../../lib/errors" }

# External

[dev-dependencies]
# None yet
