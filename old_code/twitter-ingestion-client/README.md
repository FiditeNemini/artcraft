twitter-ingestion-client
========================

Ingest events from Twitter and send them to Redis PubSub

Protos
------
Protos are defined in a git submodule:

```
git submodule add git@github.com:storytold/storyteller-protos.git
```
