## Frontend
All frontend code lives in `tts_frontend/`

###Installation
Bootstraped with `create-react-app`
```
nvm ls-remote
nvm install v12.14.1 # eg, latest lts release
nvm use v12.14.1
npm install -g npm # update
```

npm start

npm run build

